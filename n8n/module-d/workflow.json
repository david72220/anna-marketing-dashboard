{
  "name": "📈 Anna — Module D — Veille Performance Concurrents",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "weeks",
              "triggerAtDay": [
                1
              ],
              "triggerAtHour": 8
            }
          ]
        }
      },
      "id": "f060f414-671a-4107-af06-f060f414671a",
      "name": "Schedule Lundi 8h",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.3,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "anna-veille-performance",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "b5fe1e70-d105-420f-ab5f-b5fe1e70d105",
      "name": "Webhook Manuel",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        200
      ]
    },
    {
      "parameters": {
        "resource": "databasePage",
        "operation": "getAll",
        "databaseId": {
          "__rl": true,
          "mode": "url",
          "value": "https://www.notion.so/4aa33ab381b84f75a5b76511c97d7a3b"
        },
        "returnAll": true,
        "simple": false,
        "options": {}
      },
      "id": "6eb5ecd6-8ca5-4121-a6eb-6eb5ecd68ca5",
      "name": "Lire Comptes Actifs",
      "type": "n8n-nodes-base.notion",
      "typeVersion": 2,
      "position": [
        220,
        100
      ]
    },
    {
      "parameters": {
        "jsCode": "// Lit les comptes actifs et prépare les listes de handles par plateforme.\nconst comptes = $input.all().map((i) => i.json);\n\nconst actifs = comptes.filter((c) => c.properties && c.properties.Actif && c.properties.Actif.checkbox === true);\n\nconst parPlateforme = { Instagram: [], TikTok: [] };\nconst fiches = {};\n\nfor (const c of actifs) {\n  const p = c.properties || {};\n  const plateforme = p.Plateforme && p.Plateforme.select ? p.Plateforme.select.name : null;\n  const handleBrut = p.Handle && p.Handle.rich_text && p.Handle.rich_text[0] ? p.Handle.rich_text[0].plain_text : '';\n  const handle = String(handleBrut || '').toLowerCase().replace('@', '').trim();\n\n  if (!plateforme || !handle || !parPlateforme[plateforme]) continue;\n\n  parPlateforme[plateforme].push(handle);\n  fiches[plateforme + ':' + handle] = c.id;\n}\n\nconst depuis = new Date(Date.now() - 14 * 24 * 3600 * 1000).toISOString();\n\nreturn [{ json: {\n  instagram: parPlateforme.Instagram,\n  tiktok: parPlateforme.TikTok,\n  fiches,\n  depuis,\n  nbComptes: actifs.length\n} }];"
      },
      "id": "eb787461-db28-4ddd-aeb7-eb787461db28",
      "name": "Preparer Handles",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        440,
        100
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.apify.com/v2/acts/apify~instagram-scraper/run-sync-get-dataset-items",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ directUrls: $json.instagram.map(h => 'https://www.instagram.com/' + h + '/'), resultsType: 'posts', resultsLimit: 15, addParentData: false }) }}",
        "options": {
          "timeout": 300000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      },
      "id": "de3c6828-2d34-400e-ade3-de3c68282d34",
      "name": "Apify Instagram",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        660,
        100
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.apify.com/v2/acts/clockworks~tiktok-scraper/run-sync-get-dataset-items",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ profiles: $('Preparer Handles').first().json.tiktok, resultsPerPage: 15, shouldDownloadVideos: false, shouldDownloadCovers: false, shouldDownloadSubtitles: false, shouldDownloadSlideshowImages: false }) }}",
        "options": {
          "timeout": 300000,
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      },
      "id": "e378a1e0-25c4-45fc-ae37-e378a1e025c4",
      "name": "Apify TikTok",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        880,
        100
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "// Fichier généré automatiquement — NE JAMAIS ÉDITER À LA MAIN.\n// Régénérer avec : npm run build:n8n\n// Source de vérité : src/lib/veille/normalize.mjs et src/lib/veille/score.mjs,\n// couverts par leurs tests (npm test). Ce sont eux qui font foi, pas ce fichier.\n\n// ---- Origine : src/lib/veille/normalize.mjs ----\nconst ACCROCHE_MAX = 80;\n\nfunction accrocheDepuis(legende) {\n    const texte = String(legende || \"\").replace(/\\s+/g, \" \").trim();\n    if (!texte) return \"(sans légende)\";\n    return texte.slice(0, ACCROCHE_MAX);\n}\n\nfunction entier(valeur) {\n    const n = parseInt(String(valeur ?? \"0\"), 10);\n    return Number.isFinite(n) && n > 0 ? n : 0;\n}\n\nfunction typeInstagram(item) {\n    if (item.type === \"Sidecar\") return \"Carrousel\";\n    if (item.type === \"Video\" || item.productType === \"clips\") return \"Reel\";\n    return \"Photo\";\n}\n\nfunction normaliserInstagram(items) {\n    if (!Array.isArray(items)) return [];\n    const resultat = [];\n    for (const item of items) {\n        if (!item || typeof item !== \"object\" || !item.id) continue;\n        const type = typeInstagram(item);\n        resultat.push({\n            postId: String(item.id),\n            plateforme: \"Instagram\",\n            handle: String(item.ownerUsername || \"\").toLowerCase().replace(\"@\", \"\"),\n            url: item.url || (item.shortCode ? `https://www.instagram.com/p/${item.shortCode}/` : \"\"),\n            datePublication: item.timestamp || \"\",\n            type,\n            accroche: accrocheDepuis(item.caption),\n            legende: String(item.caption || \"\"),\n            vues: entier(item.videoPlayCount ?? item.videoViewCount),\n            likes: entier(item.likesCount),\n            commentaires: entier(item.commentsCount),\n            metriqueScore: type === \"Reel\" ? \"Vues\" : \"Likes\",\n        });\n    }\n    return resultat;\n}\n\nfunction normaliserTikTok(items) {\n    if (!Array.isArray(items)) return [];\n    const resultat = [];\n    for (const item of items) {\n        if (!item || typeof item !== \"object\" || !item.id) continue;\n        const auteur = item.authorMeta || {};\n        resultat.push({\n            postId: String(item.id),\n            plateforme: \"TikTok\",\n            handle: String(auteur.name || item.uniqueId || \"\").toLowerCase().replace(\"@\", \"\"),\n            url: item.webVideoUrl || \"\",\n            datePublication: item.createTimeISO || \"\",\n            type: \"TikTok\",\n            accroche: accrocheDepuis(item.text),\n            legende: String(item.text || \"\"),\n            vues: entier(item.playCount),\n            likes: entier(item.diggCount),\n            commentaires: entier(item.commentCount),\n            metriqueScore: \"Vues\",\n        });\n    }\n    return resultat;\n}\n\nfunction normaliser(plateforme, items) {\n    if (plateforme === \"Instagram\") return normaliserInstagram(items);\n    if (plateforme === \"TikTok\") return normaliserTikTok(items);\n    return [];\n}\n\n// ---- Origine : src/lib/veille/score.mjs ----\nconst TAILLE_BASELINE = 30;\nconst ECHANTILLON_MINIMUM = 10;\nconst SEUIL_SURPERFORMANCE = 1.5;\n// Six mois : au-delà, \"d'habitude\" pour ce compte n'a plus de sens (ex. compte\n// dont l'audience s'est effondrée depuis un pic viral ancien).\nconst FENETRE_BASELINE_JOURS = 183;\n\n// Notion peut renvoyer la métrique avec une casse quelconque ; toute valeur\n// inconnue retombe sur les vues, qui est le cas majoritaire.\nfunction metriqueNormalisee(post) {\n    return String(post?.metriqueScore || \"\").trim().toLowerCase() === \"likes\" ? \"Likes\" : \"Vues\";\n}\n\n// Retourne null — et non 0 — quand la valeur est absente ou illisible, pour que\n// l'entrée soit écartée de l'échantillon au lieu de le diluer.\nfunction valeurMetrique(post) {\n    const brut = metriqueNormalisee(post) === \"Likes\" ? post?.likes : post?.vues;\n    if (brut === null || brut === undefined || brut === \"\") return null;\n    const n = Number(brut);\n    return Number.isFinite(n) ? n : null;\n}\n\nfunction instant(post) {\n    const t = Date.parse(post?.datePublication);\n    return Number.isNaN(t) ? null : t;\n}\n\nfunction moyenneReference(post, historique) {\n    const vide = { moyenne: 0, echantillon: 0 };\n    if (!post || typeof post !== \"object\") return vide;\n\n    const metrique = metriqueNormalisee(post);\n    // Ancré sur la publication évaluée (pas Date.now()) : la fonction reste\n    // pure et compare une publication au contexte du compte à ce moment-là.\n    // Si la publication évaluée n'a pas de date exploitable, on ne peut pas\n    // ancrer la fenêtre : aucun filtre d'âge n'est appliqué.\n    const instantEvalue = instant(post);\n    const bornInf = instantEvalue === null ? null : instantEvalue - FENETRE_BASELINE_JOURS * 24 * 60 * 60 * 1000;\n    const vus = new Set();\n    const pertinents = [];\n\n    for (const h of Array.isArray(historique) ? historique : []) {\n        if (!h || typeof h !== \"object\") continue;\n        if (post.postId && h.postId === post.postId) continue;\n        if (h.handle !== post.handle) continue;\n        if (h.plateforme !== post.plateforme) continue;\n        if (metriqueNormalisee(h) !== metrique) continue;\n\n        const t = instant(h);\n        if (t === null) continue;\n        if (bornInf !== null && t < bornInf) continue;\n\n        const valeur = valeurMetrique(h);\n        if (valeur === null) continue;\n\n        const cle = h.postId || `${h.url || \"\"}|${t}`;\n        if (vus.has(cle)) continue;\n        vus.add(cle);\n\n        pertinents.push({ t, valeur });\n    }\n\n    if (pertinents.length === 0) return vide;\n\n    pertinents.sort((a, b) => b.t - a.t);\n    const retenus = pertinents.slice(0, TAILLE_BASELINE);\n\n    const total = retenus.reduce((somme, p) => somme + p.valeur, 0);\n    return {\n        moyenne: Math.round((total / retenus.length) * 100) / 100,\n        echantillon: retenus.length,\n    };\n}\n\nfunction calculerScore(post, historique) {\n    const { moyenne, echantillon } = moyenneReference(post, historique);\n    const fiable = echantillon >= ECHANTILLON_MINIMUM;\n    const valeur = valeurMetrique(post);\n\n    if (valeur === null || !(moyenne > 0)) {\n        return { score: null, moyenne, echantillon, fiable: false, surperforme: false };\n    }\n\n    const score = Math.round((valeur / moyenne) * 100) / 100;\n\n    return {\n        score,\n        moyenne,\n        echantillon,\n        fiable,\n        surperforme: fiable && score >= SEUIL_SURPERFORMANCE,\n    };\n}\n\nfunction repartir(collectes, pagesParPostId) {\n    const creations = [];\n    const majs = [];\n    const vus = new Set();\n    const pages = pagesParPostId && typeof pagesParPostId === \"object\" ? pagesParPostId : {};\n\n    for (const post of Array.isArray(collectes) ? collectes : []) {\n        if (!post || typeof post !== \"object\") continue;\n        if (!post.postId) continue;\n        if (vus.has(post.postId)) continue;\n        vus.add(post.postId);\n\n        const pageId = pages[post.postId];\n        if (pageId) majs.push({ post, pageId });\n        else creations.push(post);\n    }\n\n    return { creations, majs };\n}\n\n// ---- glue N8N ----\nconst prep = $('Preparer Handles').first().json;\n\nconst erreurs = [];\n\n// Avec onError: continueRegularOutput, un appel Apify qui échoue n'émet pas\n// d'exception : il émet un item { error: {...} } comme un résultat normal.\n// Sans cette détection, une panne de scraping remonte comme « 0 publication\n// collectée », c'est-à-dire un succès. C'est arrivé.\nfunction collecter(nomNoeud, plateforme) {\n  let items;\n  try {\n    items = $(nomNoeud).all().map((i) => i.json);\n  } catch (e) {\n    erreurs.push(plateforme + ' : ' + e.message);\n    return [];\n  }\n\n  const enErreur = items.filter((x) => x && x.error);\n  if (enErreur.length > 0) {\n    const detail = enErreur[0].error;\n    const message = (detail && detail.message) || JSON.stringify(detail);\n    erreurs.push(plateforme + ' : ' + String(message).slice(0, 200));\n    return [];\n  }\n\n  return items;\n}\n\nconst brutIg = collecter('Apify Instagram', 'Instagram');\nconst brutTt = collecter('Apify TikTok', 'TikTok');\n\nconst depuis = new Date(prep.depuis).getTime();\n\n// Fenêtre glissante + rattachement à la fiche compte Notion.\n// Un post dont le compte n'est pas dans le registre est écarté : il ne doit\n// pas polluer les moyennes d'un autre compte.\nconst posts = [];\nfor (const p of [...normaliserInstagram(brutIg), ...normaliserTikTok(brutTt)]) {\n  if (!p.datePublication) continue;\n  const t = Date.parse(p.datePublication);\n  if (Number.isNaN(t) || t < depuis) continue;\n\n  const pageCompte = prep.fiches[p.plateforme + ':' + p.handle];\n  if (!pageCompte) continue;\n\n  posts.push({ ...p, pageCompte });\n}\n\nreturn [{ json: { posts, erreurs, nbComptes: prep.nbComptes } }];"
      },
      "id": "7de39081-48fd-4624-a7de-7de3908148fd",
      "name": "Normaliser",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1100,
        100
      ]
    },
    {
      "parameters": {
        "resource": "databasePage",
        "operation": "getAll",
        "databaseId": {
          "__rl": true,
          "mode": "url",
          "value": "https://www.notion.so/4ad61bf0d0374dc78298d20b91a756ee"
        },
        "returnAll": true,
        "simple": false,
        "options": {}
      },
      "id": "6244b7c5-6883-4a6f-a624-6244b7c56883",
      "name": "Lire Posts Existants",
      "type": "n8n-nodes-base.notion",
      "typeVersion": 2,
      "position": [
        1320,
        100
      ]
    },
    {
      "parameters": {
        "jsCode": "// Fichier généré automatiquement — NE JAMAIS ÉDITER À LA MAIN.\n// Régénérer avec : npm run build:n8n\n// Source de vérité : src/lib/veille/normalize.mjs et src/lib/veille/score.mjs,\n// couverts par leurs tests (npm test). Ce sont eux qui font foi, pas ce fichier.\n\n// ---- Origine : src/lib/veille/normalize.mjs ----\nconst ACCROCHE_MAX = 80;\n\nfunction accrocheDepuis(legende) {\n    const texte = String(legende || \"\").replace(/\\s+/g, \" \").trim();\n    if (!texte) return \"(sans légende)\";\n    return texte.slice(0, ACCROCHE_MAX);\n}\n\nfunction entier(valeur) {\n    const n = parseInt(String(valeur ?? \"0\"), 10);\n    return Number.isFinite(n) && n > 0 ? n : 0;\n}\n\nfunction typeInstagram(item) {\n    if (item.type === \"Sidecar\") return \"Carrousel\";\n    if (item.type === \"Video\" || item.productType === \"clips\") return \"Reel\";\n    return \"Photo\";\n}\n\nfunction normaliserInstagram(items) {\n    if (!Array.isArray(items)) return [];\n    const resultat = [];\n    for (const item of items) {\n        if (!item || typeof item !== \"object\" || !item.id) continue;\n        const type = typeInstagram(item);\n        resultat.push({\n            postId: String(item.id),\n            plateforme: \"Instagram\",\n            handle: String(item.ownerUsername || \"\").toLowerCase().replace(\"@\", \"\"),\n            url: item.url || (item.shortCode ? `https://www.instagram.com/p/${item.shortCode}/` : \"\"),\n            datePublication: item.timestamp || \"\",\n            type,\n            accroche: accrocheDepuis(item.caption),\n            legende: String(item.caption || \"\"),\n            vues: entier(item.videoPlayCount ?? item.videoViewCount),\n            likes: entier(item.likesCount),\n            commentaires: entier(item.commentsCount),\n            metriqueScore: type === \"Reel\" ? \"Vues\" : \"Likes\",\n        });\n    }\n    return resultat;\n}\n\nfunction normaliserTikTok(items) {\n    if (!Array.isArray(items)) return [];\n    const resultat = [];\n    for (const item of items) {\n        if (!item || typeof item !== \"object\" || !item.id) continue;\n        const auteur = item.authorMeta || {};\n        resultat.push({\n            postId: String(item.id),\n            plateforme: \"TikTok\",\n            handle: String(auteur.name || item.uniqueId || \"\").toLowerCase().replace(\"@\", \"\"),\n            url: item.webVideoUrl || \"\",\n            datePublication: item.createTimeISO || \"\",\n            type: \"TikTok\",\n            accroche: accrocheDepuis(item.text),\n            legende: String(item.text || \"\"),\n            vues: entier(item.playCount),\n            likes: entier(item.diggCount),\n            commentaires: entier(item.commentCount),\n            metriqueScore: \"Vues\",\n        });\n    }\n    return resultat;\n}\n\nfunction normaliser(plateforme, items) {\n    if (plateforme === \"Instagram\") return normaliserInstagram(items);\n    if (plateforme === \"TikTok\") return normaliserTikTok(items);\n    return [];\n}\n\n// ---- Origine : src/lib/veille/score.mjs ----\nconst TAILLE_BASELINE = 30;\nconst ECHANTILLON_MINIMUM = 10;\nconst SEUIL_SURPERFORMANCE = 1.5;\n// Six mois : au-delà, \"d'habitude\" pour ce compte n'a plus de sens (ex. compte\n// dont l'audience s'est effondrée depuis un pic viral ancien).\nconst FENETRE_BASELINE_JOURS = 183;\n\n// Notion peut renvoyer la métrique avec une casse quelconque ; toute valeur\n// inconnue retombe sur les vues, qui est le cas majoritaire.\nfunction metriqueNormalisee(post) {\n    return String(post?.metriqueScore || \"\").trim().toLowerCase() === \"likes\" ? \"Likes\" : \"Vues\";\n}\n\n// Retourne null — et non 0 — quand la valeur est absente ou illisible, pour que\n// l'entrée soit écartée de l'échantillon au lieu de le diluer.\nfunction valeurMetrique(post) {\n    const brut = metriqueNormalisee(post) === \"Likes\" ? post?.likes : post?.vues;\n    if (brut === null || brut === undefined || brut === \"\") return null;\n    const n = Number(brut);\n    return Number.isFinite(n) ? n : null;\n}\n\nfunction instant(post) {\n    const t = Date.parse(post?.datePublication);\n    return Number.isNaN(t) ? null : t;\n}\n\nfunction moyenneReference(post, historique) {\n    const vide = { moyenne: 0, echantillon: 0 };\n    if (!post || typeof post !== \"object\") return vide;\n\n    const metrique = metriqueNormalisee(post);\n    // Ancré sur la publication évaluée (pas Date.now()) : la fonction reste\n    // pure et compare une publication au contexte du compte à ce moment-là.\n    // Si la publication évaluée n'a pas de date exploitable, on ne peut pas\n    // ancrer la fenêtre : aucun filtre d'âge n'est appliqué.\n    const instantEvalue = instant(post);\n    const bornInf = instantEvalue === null ? null : instantEvalue - FENETRE_BASELINE_JOURS * 24 * 60 * 60 * 1000;\n    const vus = new Set();\n    const pertinents = [];\n\n    for (const h of Array.isArray(historique) ? historique : []) {\n        if (!h || typeof h !== \"object\") continue;\n        if (post.postId && h.postId === post.postId) continue;\n        if (h.handle !== post.handle) continue;\n        if (h.plateforme !== post.plateforme) continue;\n        if (metriqueNormalisee(h) !== metrique) continue;\n\n        const t = instant(h);\n        if (t === null) continue;\n        if (bornInf !== null && t < bornInf) continue;\n\n        const valeur = valeurMetrique(h);\n        if (valeur === null) continue;\n\n        const cle = h.postId || `${h.url || \"\"}|${t}`;\n        if (vus.has(cle)) continue;\n        vus.add(cle);\n\n        pertinents.push({ t, valeur });\n    }\n\n    if (pertinents.length === 0) return vide;\n\n    pertinents.sort((a, b) => b.t - a.t);\n    const retenus = pertinents.slice(0, TAILLE_BASELINE);\n\n    const total = retenus.reduce((somme, p) => somme + p.valeur, 0);\n    return {\n        moyenne: Math.round((total / retenus.length) * 100) / 100,\n        echantillon: retenus.length,\n    };\n}\n\nfunction calculerScore(post, historique) {\n    const { moyenne, echantillon } = moyenneReference(post, historique);\n    const fiable = echantillon >= ECHANTILLON_MINIMUM;\n    const valeur = valeurMetrique(post);\n\n    if (valeur === null || !(moyenne > 0)) {\n        return { score: null, moyenne, echantillon, fiable: false, surperforme: false };\n    }\n\n    const score = Math.round((valeur / moyenne) * 100) / 100;\n\n    return {\n        score,\n        moyenne,\n        echantillon,\n        fiable,\n        surperforme: fiable && score >= SEUIL_SURPERFORMANCE,\n    };\n}\n\nfunction repartir(collectes, pagesParPostId) {\n    const creations = [];\n    const majs = [];\n    const vus = new Set();\n    const pages = pagesParPostId && typeof pagesParPostId === \"object\" ? pagesParPostId : {};\n\n    for (const post of Array.isArray(collectes) ? collectes : []) {\n        if (!post || typeof post !== \"object\") continue;\n        if (!post.postId) continue;\n        if (vus.has(post.postId)) continue;\n        vus.add(post.postId);\n\n        const pageId = pages[post.postId];\n        if (pageId) majs.push({ post, pageId });\n        else creations.push(post);\n    }\n\n    return { creations, majs };\n}\n\n// ---- glue N8N ----\nconst collecte = $('Normaliser').first().json;\nconst pagesNotion = $input.all().map((i) => i.json).filter((p) => p && p.properties);\n\nconst txt = (prop) => {\n  if (!prop || !Array.isArray(prop.rich_text)) return '';\n  return prop.rich_text.map((t) => t.plain_text || (t.text && t.text.content) || '').join('');\n};\nconst nombre = (prop) => (prop && typeof prop.number === 'number' ? prop.number : null);\nconst selectNom = (prop) => (prop && prop.select ? prop.select.name : '');\nconst dateDebut = (prop) => (prop && prop.date ? prop.date.start : '');\n\n// Historique = ce qui est déjà en base. Le Handle est indispensable ici :\n// l'URL d'un post Instagram (/p/CODE/) ne contient pas le nom du compte.\nconst historique = pagesNotion.map((page) => ({\n  postId: txt(page.properties['Post ID']),\n  pageId: page.id,\n  handle: txt(page.properties['Handle']),\n  plateforme: selectNom(page.properties['Plateforme']),\n  metriqueScore: selectNom(page.properties['Métrique de score']) || 'Vues',\n  vues: nombre(page.properties['Vues']),\n  likes: nombre(page.properties['Likes']),\n  datePublication: dateDebut(page.properties['Date publication'])\n})).filter((h) => h.postId);\n\nconst pagesParPostId = {};\nfor (const h of historique) pagesParPostId[h.postId] = h.pageId;\n\n// Historique de travail = base + lot courant, pour que les publications de la\n// semaine se comparent aussi entre elles. Les compteurs des posts déjà connus\n// sont rafraîchis avec les valeurs qui viennent d'être collectées.\nconst parId = new Map();\nfor (const h of historique) parId.set(h.postId, h);\nfor (const p of collecte.posts) {\n  const ancien = parId.get(p.postId);\n  parId.set(p.postId, ancien ? { ...ancien, vues: p.vues, likes: p.likes } : p);\n}\nconst historiqueComplet = [...parId.values()];\n\nconst scores = collecte.posts.map((p) => ({ ...p, ...calculerScore(p, historiqueComplet) }));\nconst reparti = repartir(scores, pagesParPostId);\n\n// Moyennes par fiche compte, calculées séparément par métrique.\n// On repart de TOUS les comptes actifs, pas seulement de ceux qui ont produit\n// des publications : un compte dormant doit voir sa date de collecte mise à\n// jour, sinon rien ne distingue « interrogé, rien de neuf » de « jamais\n// interrogé ». Un compte sans publication récente ressort avec nbBaseline à 0.\nconst prep = $('Preparer Handles').first().json;\nconst comptesVus = {};\nfor (const cle of Object.keys(prep.fiches || {})) {\n  const separateur = cle.indexOf(':');\n  comptesVus[prep.fiches[cle]] = {\n    plateforme: cle.slice(0, separateur),\n    handle: cle.slice(separateur + 1)\n  };\n}\n\nconst moyenne = (valeurs) => (valeurs.length ? Math.round(valeurs.reduce((a, b) => a + b, 0) / valeurs.length) : 0);\n\nconst majComptes = Object.keys(comptesVus).map((pageId) => {\n  const info = comptesVus[pageId];\n  const duCompte = historiqueComplet.filter((h) => h.handle === info.handle && h.plateforme === info.plateforme);\n  return {\n    pageId,\n    moyenneVues: moyenne(duCompte.filter((h) => typeof h.vues === 'number' && h.vues > 0).map((h) => h.vues)),\n    moyenneLikes: moyenne(duCompte.filter((h) => typeof h.likes === 'number' && h.likes > 0).map((h) => h.likes)),\n    nbBaseline: duCompte.length\n  };\n});\n\nconst surperformants = scores.filter((s) => s.surperforme).sort((a, b) => b.score - a.score);\n\nreturn [{ json: {\n  creations: reparti.creations,\n  majs: reparti.majs,\n  majComptes,\n  surperformants,\n  erreurs: collecte.erreurs,\n  aujourdhui: new Date().toISOString().split('T')[0],\n  total: scores.length,\n  nbCreations: reparti.creations.length,\n  nbMajs: reparti.majs.length,\n  nbSurperformants: surperformants.length\n} }];"
      },
      "id": "df495c55-c8cb-40bf-adf4-df495c55c8cb",
      "name": "Scorer",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        100
      ]
    },
    {
      "parameters": {
        "jsCode": "// Un item par page à créer. Tableau vide => le nœud Notion en aval ne tourne pas.\nconst s = $('Scorer').first().json;\nreturn (s.creations || []).map((p) => ({ json: { ...p, collecteLe: s.aujourdhui } }));"
      },
      "id": "37bc90e3-888e-47a5-a37b-37bc90e3888e",
      "name": "Filtrer Creations",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        -100
      ]
    },
    {
      "parameters": {
        "jsCode": "// Un item par page à mettre à jour, avec son identifiant de page Notion.\nconst s = $('Scorer').first().json;\nreturn (s.majs || []).map((m) => ({ json: { ...m.post, pageId: m.pageId, collecteLe: s.aujourdhui } }));"
      },
      "id": "23c2b1a6-c5a0-489d-a23c-23c2b1a6c5a0",
      "name": "Filtrer Majs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        100
      ]
    },
    {
      "parameters": {
        "jsCode": "// Un item par fiche compte dont les moyennes sont à rafraîchir.\nconst s = $('Scorer').first().json;\nreturn (s.majComptes || []).map((c) => ({ json: { ...c, collecteLe: s.aujourdhui } }));"
      },
      "id": "a0a42a8b-02dd-4316-aa0a-a0a42a8b02dd",
      "name": "Filtrer Comptes",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1760,
        300
      ]
    },
    {
      "parameters": {
        "resource": "databasePage",
        "databaseId": {
          "__rl": true,
          "mode": "url",
          "value": "https://www.notion.so/4ad61bf0d0374dc78298d20b91a756ee"
        },
        "title": "={{ $json.accroche }}",
        "simple": false,
        "propertiesUi": {
          "propertyValues": [
            {
              "key": "Compte|relation",
              "relationValue": [
                "={{ $json.pageCompte }}"
              ]
            },
            {
              "key": "Handle|rich_text",
              "richText": true,
              "text": {
                "text": [
                  {
                    "text": "={{ $json.handle }}",
                    "annotationUi": {}
                  }
                ]
              }
            },
            {
              "key": "Plateforme|select",
              "selectValue": "={{ $json.plateforme }}"
            },
            {
              "key": "Type|select",
              "selectValue": "={{ $json.type }}"
            },
            {
              "key": "URL post|url",
              "urlValue": "={{ $json.url }}"
            },
            {
              "key": "Date publication|date",
              "includeTime": false,
              "date": "={{ $json.datePublication }}"
            },
            {
              "key": "Vues|number",
              "numberValue": "={{ $json.vues }}"
            },
            {
              "key": "Likes|number",
              "numberValue": "={{ $json.likes }}"
            },
            {
              "key": "Commentaires|number",
              "numberValue": "={{ $json.commentaires }}"
            },
            {
              "key": "Métrique de score|select",
              "selectValue": "={{ $json.metriqueScore }}"
            },
            {
              "key": "Score surperformance|number",
              "numberValue": "={{ $json.score }}"
            },
            {
              "key": "Post ID|rich_text",
              "richText": true,
              "text": {
                "text": [
                  {
                    "text": "={{ $json.postId }}",
                    "annotationUi": {}
                  }
                ]
              }
            },
            {
              "key": "Collecté le|date",
              "includeTime": false,
              "date": "={{ $json.collecteLe }}"
            }
          ]
        },
        "options": {}
      },
      "id": "9bce01df-6e86-456f-a9bc-9bce01df6e86",
      "name": "Creer Posts",
      "type": "n8n-nodes-base.notion",
      "typeVersion": 2,
      "position": [
        1980,
        -100
      ]
    },
    {
      "parameters": {
        "resource": "databasePage",
        "operation": "update",
        "pageId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $json.pageId }}"
        },
        "simple": false,
        "propertiesUi": {
          "propertyValues": [
            {
              "key": "Vues|number",
              "numberValue": "={{ $json.vues }}"
            },
            {
              "key": "Likes|number",
              "numberValue": "={{ $json.likes }}"
            },
            {
              "key": "Commentaires|number",
              "numberValue": "={{ $json.commentaires }}"
            },
            {
              "key": "Score surperformance|number",
              "numberValue": "={{ $json.score }}"
            },
            {
              "key": "Handle|rich_text",
              "richText": true,
              "text": {
                "text": [
                  {
                    "text": "={{ $json.handle }}",
                    "annotationUi": {}
                  }
                ]
              }
            },
            {
              "key": "Collecté le|date",
              "includeTime": false,
              "date": "={{ $json.collecteLe }}"
            }
          ]
        },
        "options": {}
      },
      "id": "71b8de78-73c2-4c3d-a71b-71b8de7873c2",
      "name": "Maj Posts",
      "type": "n8n-nodes-base.notion",
      "typeVersion": 2,
      "position": [
        1980,
        100
      ]
    },
    {
      "parameters": {
        "resource": "databasePage",
        "operation": "update",
        "pageId": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $json.pageId }}"
        },
        "simple": false,
        "propertiesUi": {
          "propertyValues": [
            {
              "key": "Moyenne vues|number",
              "numberValue": "={{ $json.moyenneVues }}"
            },
            {
              "key": "Moyenne likes|number",
              "numberValue": "={{ $json.moyenneLikes }}"
            },
            {
              "key": "Nb posts baseline|number",
              "numberValue": "={{ $json.nbBaseline }}"
            },
            {
              "key": "Dernière collecte|date",
              "includeTime": false,
              "date": "={{ $json.collecteLe }}"
            }
          ]
        },
        "options": {}
      },
      "id": "748e2956-e305-41fb-a748-748e2956e305",
      "name": "Maj Comptes",
      "type": "n8n-nodes-base.notion",
      "typeVersion": 2,
      "position": [
        1980,
        300
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ total: $json.total, creations: $json.nbCreations, majs: $json.nbMajs, surperformants: $json.nbSurperformants, erreurs: $json.erreurs }) }}",
        "options": {}
      },
      "id": "75488984-55f1-45a4-a754-7548898455f1",
      "name": "Reponse Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        1760,
        500
      ]
    }
  ],
  "connections": {
    "Schedule Lundi 8h": {
      "main": [
        [
          {
            "node": "Lire Comptes Actifs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook Manuel": {
      "main": [
        [
          {
            "node": "Lire Comptes Actifs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Lire Comptes Actifs": {
      "main": [
        [
          {
            "node": "Preparer Handles",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Preparer Handles": {
      "main": [
        [
          {
            "node": "Apify Instagram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apify Instagram": {
      "main": [
        [
          {
            "node": "Apify TikTok",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Apify TikTok": {
      "main": [
        [
          {
            "node": "Normaliser",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normaliser": {
      "main": [
        [
          {
            "node": "Lire Posts Existants",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Lire Posts Existants": {
      "main": [
        [
          {
            "node": "Scorer",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Scorer": {
      "main": [
        [
          {
            "node": "Filtrer Creations",
            "type": "main",
            "index": 0
          },
          {
            "node": "Filtrer Majs",
            "type": "main",
            "index": 0
          },
          {
            "node": "Filtrer Comptes",
            "type": "main",
            "index": 0
          },
          {
            "node": "Reponse Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filtrer Creations": {
      "main": [
        [
          {
            "node": "Creer Posts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filtrer Majs": {
      "main": [
        [
          {
            "node": "Maj Posts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filtrer Comptes": {
      "main": [
        [
          {
            "node": "Maj Comptes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
